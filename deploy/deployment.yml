apiVersion: apps/v1
kind: Deployment
metadata:
  name: jenkins-python
  labels:
    app: python
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python
  template:
    metadata:
      labels:
        app: python
    spec:
      containers:
      - name: todo
        image: bindu039/jenkins-python:26
        ports:
        - containerPort: 8000