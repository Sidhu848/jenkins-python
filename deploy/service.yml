apiVersion: v1
kind: Service
metadata:
  name: jenkins-python
spec:
  type: NodePort
  ports:
  - name: http
    port: 5000
    targetPort: 5000
    protocol: TCP
    nodePort: 30050 
  selector:
    app: python